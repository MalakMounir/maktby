# مكتبي (Maktby)

منصة للعثور على مساحات العمل عن بُعد وحجزها.

## المميزات

- 🏠 **الصفحة الرئيسية**: تصميم حديث مع قسم بطولي وشريط بحث
- 📋 **صفحة القوائم**: عرض جميع مساحات العمل المتاحة مع فلاتر وترتيب
- 📸 **صفحة التفاصيل**: معرض صور وتفاصيل كاملة مع نموذج حجز
- 💳 **صفحة الحجز**: نموذج دفع كامل مع ملخص الطلب

## التقنيات المستخدمة

- React 18
- React Router DOM
- Vite
- CSS3 (RTL Support)

## التثبيت والتشغيل

```bash
# تثبيت الحزم
npm install

# تشغيل المشروع في وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# معاينة البناء
npm run preview
```

## البنية

```
src/
├── components/      # المكونات المشتركة
│   └── Header.jsx
├── pages/          # صفحات التطبيق
│   ├── HomePage.jsx
│   ├── ListingsPage.jsx
│   ├── ListingDetailPage.jsx
│   └── BookingPage.jsx
├── data/           # البيانات الوهمية
│   └── mockData.js
├── App.jsx         # المكون الرئيسي
├── main.jsx        # نقطة الدخول
└── index.css       # الأنماط العامة
```

## النشر على GitHub Pages

### المتطلبات الأولية

1. تأكد من أن المشروع موجود في مستودع GitHub
2. إذا كان اسم المستودع مختلف عن `maktby`، قم بتحديث `base` في `vite.config.js`

### خطوات النشر

```bash
# 1. تأكد من أن جميع التغييرات محفوظة ومرفوعة إلى GitHub
git add .
git commit -m "Prepare for deployment"
git push origin main

# 2. قم بنشر المشروع
npm run deploy
```

### إعداد GitHub Pages

1. اذهب إلى إعدادات المستودع على GitHub (Settings)
2. انتقل إلى قسم "Pages" في القائمة الجانبية
3. في "Source"، اختر "Deploy from a branch"
4. اختر الفرع `gh-pages` والمجلد `/ (root)`
5. احفظ التغييرات

### الوصول إلى الموقع

بعد النشر، سيكون موقعك متاحاً على:
- `https://[username].github.io/maktby/`

**ملاحظة**: قد يستغرق النشر بضع دقائق حتى يصبح الموقع متاحاً.

### تحديث الموقع

عند إجراء أي تغييرات، قم بتشغيل:
```bash
npm run deploy
```

## الملاحظات

- المشروع يعمل بدون خادم خلفي (Frontend Only)
- البيانات الحالية هي بيانات وهمية (Mock Data)
- التصميم متجاوب ويعمل على جميع الأجهزة
- اللغة الأساسية هي العربية مع دعم RTL
- تم استخدام HashRouter لدعم GitHub Pages

